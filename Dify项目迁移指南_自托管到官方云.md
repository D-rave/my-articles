# Dify 项目迁移指南：自托管 → 官方云

> 适用场景：将本地或自托管服务器上的 Dify 项目，迁移到 Dify 官方云（cloud.dify.ai）新账号。

---

## 一、迁移概述

Dify 官方云目前**不支持一键全量迁移**，需要分模块手动操作。核心思路：

| 迁移内容 | 方法 | 是否完整保留 |
|---------|------|-------------|
| **应用配置（提示词、工作流、变量）** | DSL 导出/导入 | ✅ 完整保留 |
| **知识库向量数据** | 无法迁移，需重建 | ❌ 需重新上传文档并 Embedding |
| **模型 API Keys** | 手动重新配置 | ❌ 不包含在 DSL 中，需重新填写 |
| **外部调用 URL** | 云端重新生成 | ❌ 地址会变，需更新调用方 |
| **工作流中的 HTTP 外部节点** | 检查并重新测试 | ⚠️ 需手动验证连通性 |

---

## 二、应用迁移：DSL 导出与导入

### 2.1 从原环境导出 DSL

1. 登录你的 Dify（本地或自托管）
2. 进入要迁移的**应用**
3. 点击右上角 **「…」**（更多）→ **「导出 DSL」**
4. 下载得到一个 `.yml` 文件

> **注意**：DSL 文件仅包含应用编排逻辑（提示词、工作流、变量等），**不包含 API Keys、知识库向量数据、敏感凭证**。

### 2.2 导入到 Dify 官方云

1. 登录 [Dify 官方云](https://cloud.dify.ai)
2. 点击 **「创建空白应用」** 旁边的 **「…」**
3. 选择 **「导入 DSL 文件」**
4. 上传刚才下载的 `.yml` 文件
5. 应用的所有配置（提示词、工作流、变量）完整恢复

---

## 三、知识库迁移：重新上传

**知识库向量数据无法直接导出迁移**，因为本地和云端的向量数据库是隔离的。

### 操作步骤

1. 在 Dify 官方云新建**同名知识库**（方便应用关联时识别）
2. 把本地知识库里的**原始文档**重新上传到云端
3. 配置相同的分段规则（自动分段 / 自定义分段 / 父子分段）
4. 等待云端重新 Embedding（生成向量）

> **提示**：如果文档很多，建议先在本地把原始文件打包整理好，批量上传。

---

## 四、重新配置模型和 API Key

DSL 不导出任何 API Key，导入后必须手动重新配置：

1. 进入云端应用 → **「编排」** → **「模型设置」**
2. 重新选择模型（如 GPT-4o、Claude 3.5 等）
3. 在 **「设置」→「模型提供商」** 中添加你的 API Key
4. 如果应用使用了**工具**（如联网搜索、代码执行），也需要重新授权

> **注意**：如果原应用调用了 OpenAI API，而你的服务器 IP 被 OpenAI 限制（如香港 IP），在官方云（服务器在国外）通常可以直接访问，无需额外代理。

---

## 五、检查工作流中的外部节点

如果你的工作流中使用了 **HTTP 请求节点**（如调用 PaddleOCR、自定义 API、数据库等），需要逐一检查：

### 5.1 检查外部服务是否可达

- 确认被调用的外部服务（如 PaddleOCR）是否已启动
- 确认服务监听的 IP 和端口正确（如 `0.0.0.0:5000` 而非 `127.0.0.1:5000`）
- 确认服务器防火墙/安全组放行了对应端口

### 5.2 检查请求配置

- URL 地址是否正确

- Header 中的认证信息（Token、API Key）是否过期或需要更新

- 请求体 JSON 格式、字段名是否正确

- 找到对应的 PaddleOCR

- ```
  cd /opt/paddleocr-api
  nohup python3 app.py > ocr.log 2>&1 &
  ```

  才可以运行

### 5.3 测试运行

在云端重新运行工作流，观察每个节点的执行状态，确保外部调用正常返回。

---

## 六、重新发布与获取访问链接

### 6.1 更新发布

1. 进入云端应用，点击右上角 **「发布」** → **「更新发布」**
2. 确保当前版本已发布

### 6.2 获取公开访问链接（Web 应用）

1. 在「访问点」页面，找到 **「Web 应用」**
2. 复制 **Web 应用 URL**，例如：
   ```
   https://udify.app/workflow/xxxxx
   ```
3. 将此链接发给任何人，对方无需登录即可直接使用

### 6.3 获取 API 接口（后端集成）

1. 在「访问点」页面，找到 **「后端服务 API」**
2. 点击 **「新建 API Key」**，复制生成的 Key
3. 使用页面提供的 API Endpoint（如 `https://api.dify.ai/v1`）进行开发集成

> **注意**：云端导入后会生成**新的 API Key 和 Endpoint URL**，如果外部系统之前调用的是本地地址，需要全部更新为云端的。

---

## 七、常见问题排查

| 问题 | 原因 | 解决 |
|------|------|------|
| 导出链接为空 / DSL 导入失败 | 节点缺少 UUID 等关键字段 | 在原环境检查节点配置是否完整 |
| 导入后模型报错 404/503 | 模型 API Key 无效或 IP 被限制 | 在云端重新配置有效的 API Key |
| 工作流中 HTTP 节点报 503 | 外部服务未启动或不可达 | 检查被调用的服务是否运行，端口是否开放 |
| 知识库回答不准确 | 向量数据未重建 | 重新上传原文档，等待 Embedding 完成 |
| 别人打不开 Web 链接 | 链接是本地内网地址 | 确保发布的是官方云链接（udify.app）或服务器公网 IP |

---

## 八、总结流程图

```
原 Dify 环境
    │
    ├─ 导出应用 DSL (.yml)
    ├─ 打包知识库原始文档
    └─ 记录外部 API 地址和认证信息
           │
           ▼
Dify 官方云（cloud.dify.ai）
    │
    ├─ 导入 DSL 文件
    ├─ 新建知识库 → 上传文档 → 重新 Embedding
    ├─ 重新配置模型 API Key
    ├─ 检查工作流 HTTP 节点连通性
    └─ 发布 → 复制 Web 链接 / API Key
```

---

*文档整理时间：2026-09-01*
