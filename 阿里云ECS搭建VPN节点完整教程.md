# 阿里云 ECS 搭建 Xray VPN 节点完整教程

> 适用场景：通过阿里云香港/海外 ECS 实例，搭建个人 VPN 节点，访问国际互联网。
> 服务端：3X-UI 面板 + Xray 核心 | 客户端：v2rayN (Windows)

---

## 一、准备工作

### 1.1 购买/准备阿里云 ECS
- **地域**：中国香港 (`cn-hongkong`) 或其他海外地域
- **系统**：Ubuntu 22.04 LTS
- **配置**：1核2G 或 2核4G 即可（仅做代理，不需要高配置）
- **网络**：必须分配 **公网 IPv4 地址**
- **登录凭证**：建议设置 root 密码（或密钥对）

### 1.2 配置安全组（入方向）

登录阿里云控制台 → ECS → 实例 → 网络与安全组 → 配置规则，添加：

| 协议类型 | 端口范围 | 授权对象 | 说明 |
|---------|---------|---------|------|
| 自定义 TCP | `54321` | `你的公网IP/32` | 3X-UI 管理面板（限制来源IP，防止被扫描） |
| 自定义 TCP | `442` | `0.0.0.0/0` | Xray 代理端口（客户端连接用） |
| 自定义 TCP | `22` | `你的公网IP/32` | SSH 远程连接（限制来源IP，更安全） |

> **注意**：`你的公网IP` 可通过 https://ip.sb 查询。如果家用宽带重启后 IP 会变，SSH 连不上时需回控制台修改。

---

## 二、服务端安装 3X-UI

### 2.1 SSH 连接服务器

```bash
ssh root@<你的服务器公网IP>
```

### 2.2 一键安装 3X-UI

```bash
bash <(curl -Ls https://raw.githubusercontent.com/mhsanaei/3x-ui/master/install.sh)
```

安装过程中按提示选择：

| 提示 | 选择 |
|------|------|
| Database Selection | `1` SQLite（个人使用推荐） |
| 自定义面板端口 | `y` → 输入 `54321` |
| SSL 证书设置 | `4` Skip SSL（HTTP 访问面板即可） |
| 绑定到 127.0.0.1 | `N`（默认，允许公网访问面板） |

安装完成后会显示访问地址、用户名、密码。

### 2.3 安装 sqlite3（后续修复用）

```bash
apt install -y sqlite3
```

---

## 三、登录面板并创建节点

### 3.1 访问面板

```
x-ui settings
```

会生成访问页面

```
http://<你的服务器IP>:54321/<随机路径>
```

### 3.2 创建入站节点

1. 左侧菜单 → **入站列表** → **+ 添加入站**
2. 填写基础配置：

| 配置项 | 值 |
|--------|-----|
| 备注 | `香港节点`（任意） |
| 协议 | `VLESS` |
| 端口 | `442`（或你安全组放行的端口） |

3. 切换到 **「协议」** 标签页：
   - 解密：`none`
   - 加密：`none`
   - **UUID**：如果面板没有显示 UUID 输入框，先点击「创建」，后续通过命令行修复（见第 4 章）

4. 切换到 **「传输」** 标签页：
   - 传输：`RAW`（即 TCP）

5. 切换到 **「安全」** 标签页：
   - 安全：`无`

6. 点击 **「创建」**

---

## 四、修复 UUID 和 Email（关键步骤）

> 3X-UI 某些版本在创建节点时不会自动生成 UUID 和 email，导致 Xray 配置无效，客户端连接不上。

### 4.1 生成 UUID

在服务器终端执行：

```bash
uuidgen
```

记录输出，例如：`fe0534c0-8382-4ffd-afba-cedb7abb9a00`

### 4.2 写入 UUID

```bash
sqlite3 /etc/x-ui/x-ui.db "UPDATE inbounds SET settings = json_set(settings, '$.clients[0].id', '你的UUID') WHERE id = 1;"
```

### 4.3 写入 Email

VLESS 协议要求每个客户端必须有 email 标识：

```bash
sqlite3 /etc/x-ui/x-ui.db "UPDATE inbounds SET settings = json_set(settings, '$.clients[0].email', 'user1') WHERE id = 1;"
```

### 4.4 重启面板

```bash
x-ui restart
```

### 4.5 验证端口监听

```bash
ss -tlnp | grep 442
```

正常应输出类似：
```
LISTEN 0  4096  *:442  *:*  users:(("xray-linux-amd64",pid=xxx,fd=x))
```

---

## 五、客户端配置（Windows）

### 5.1 下载 v2rayN

- 访问：https://github.com/2dust/v2rayN/releases
- 下载 **`v2rayN-windows-64.zip`**（注意：不是 `arm64`，不是 `desktop`，不是 `86`）
- 解压到任意文件夹，运行 `v2rayN.exe`

### 5.2 获取节点链接

1. 浏览器打开 3X-UI 面板
2. 入站列表 → 「香港节点」→ 点击 **「操作」**（三个点）
3. 选择 **「导出链接」** → **「复制」**
4. 粘贴到记事本确认，格式应为：
   ```
   vless://<UUID>@<服务器IP>:442?type=tcp&security=none#香港节点
   ```

### 5.3 导入节点

1. 打开 v2rayN
2. 在节点列表空白处 **右键** → **「从剪贴板导入批量 URL」**
3. 节点会出现在列表中，选中后 **按回车** 设为活动服务器

### 5.4 开启代理

- 点击底部 **「系统代理」** 下拉框 → 选择 **「自动配置系统代理」**
- 图标变红表示已连接

### 5.5 切换路由模式（如遇连接问题）

如果开启代理后浏览器提示「代理服务器可能有问题」：

1. 点击底部 **「路由」** 下拉框
2. 将 `V4-绕过大陆(Whitelist)` 改为 **`V1-全局(Global)`**
3. 全局模式下所有流量走代理，最稳定

---

## 六、验证

打开浏览器，访问：

- `https://www.google.com` — 能打开即成功
- `https://ip.sb` — 显示你的服务器 IP（如 `47.238.230.209`）即流量已走代理

---

## 七、常见问题排查

| 问题 | 原因 | 解决 |
|------|------|------|
| SSH 连不上 | 安全组 22 端口限制了你当前的 IP | 去阿里云控制台把 22 端口临时改为 `0.0.0.0/0` |
| 面板打不开 | 安全组 54321 端口 IP 限制失效 | 同上，临时放开或更新为你的新公网 IP |
| v2rayN 提示「无法运行」| 下载了 ARM64 版本 | 重新下载 `v2rayN-windows-64.zip` |
| 导出链接为空 | 节点缺少 UUID | 按第 4 章用 sqlite3 写入 UUID 和 email |
| 节点延迟测试正常，但浏览器没网 | 绕过大陆模式 DNS/规则问题 | 切换为「全局」模式 |
| 开启代理后全断网 | 系统代理配置冲突 | 尝试开启 v2rayN 的「启用 Tun」模式 |

---

## 八、安全建议

1. **面板端口 54321 务必限制来源 IP**，不要开放给全网
2. **SSH 端口 22 限制来源 IP**，防止暴力破解
3. **代理端口 442 可以开放全网**，但要保管好节点链接，不要公开分享
4. 定期在 3X-UI 面板查看流量，发现异常及时更换 UUID

---

*文档生成时间：2026-09-01*
